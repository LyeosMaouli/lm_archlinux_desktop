---
- name: Install base system packages
  pacman:
    name: "{{ base_packages }}"
    state: present
    update_cache: true

- name: Configure systemd-boot
  include_tasks: systemd-boot.yml
  when: configure_systemd_boot | default(true)

- name: Configure locale and timezone
  include_tasks: locale.yml

- name: Install and configure AUR helper
  include_tasks: aur.yml
  when: install_aur_helper | default(true)

- name: Configure network
  include_tasks: network.yml
